name: Raisintine

env:
  MIRROR_URL: 'git@github.com:EpitechPromo2026/B-DOP-200-RUN-2-1-chocolatine-raphael.turpin.git'
  EXECUTABLES: ''

on: push

jobs:
  check_repository_cleanliness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Detect File
        run: find -name "*~" -printf "::error title=Unwanted file Detected,file=%f::%f\n"
        # run: echo "::set-output name=FILES::$(find -name "*~" -name "#*#" -name "*.a" -name "*.o" -name "*.so" -name "*.gcov" -name "*.gcda" -name "*.gcno" -iname "*pain_au_chocolat*" -path "" "